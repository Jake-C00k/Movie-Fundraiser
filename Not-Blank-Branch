#include <iostream>

using namespace std;
bool notBlank(string &x, string y)
{
    getline(cin , x);
    while (x.length() ==0 )
    {
        cout << y << endl;
        getline(cin, x);
    }
    return true;
}
